@"
# 기본 환경 설정
NODE_ENV=development
PORT=31002
PROJECT_NAME=MCP_Vscode_desktop
CLAUDE_API_KEY=sk-xxxxx-placeholder
"@ | Out-File ".env" -Encoding utf8
Write-Host "[INFO] .env 파일 생성 완료" -ForegroundColor Green
